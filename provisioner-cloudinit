# Check inputs exist
: "${PROVISIONER_BIN_URL:?is not defined}"
: "${ROLES:?is not defined}"
: "${CORE_IP_OPT:?is not defined}"
: "${HOSTNAME:?is not defined}"

# HOSTNAME_OPT="--hostname $HOSTNAME"

# if [ -n "${CORE_IP}" ]; CORE_IP_OPT="--core-ip $CORE_IP"
# else  CORE_IP_OPT=""; fi

# if [ -n "${PASSWORDLESSSUDO}" ]; PASSWORDLESSSUDO_OPT="--passwordlesssudo"
# else PASSWORDLESSSUDO_OPT=""; fi

# Variables
# PROVISIONER_BIN_NAME=$(basename $PROVISIONER_BIN_URL)
# PROVISIONER_DL_DIR="/tmp"
# PROVISIONER_PATH="${PROVISIONER_DL_DIR}/${PROVISIONER_BIN_NAME}"

# Download the provisioner
# wget $PROVISIONER_BIN_URL -o $PROVISIONER_PATH

# Run the provisioner
chown u+x $PROVISIONER_PATH
# $PROVISIONER_PATH $CORE_IP_OPT $HOSTNAME_OPT $PASSWORDLESSSUDO_OPT $ROLES

apt-get update
apt-get install -y zsh
